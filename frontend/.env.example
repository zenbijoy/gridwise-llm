# GridWise Frontend API URL
# Leave empty to use current origin (recommended when served behind Nginx or Vite dev proxy)
# Or specify explicit FastAPI backend URL (e.g. http://localhost:8000 or https://api.yourdomain.com)
VITE_API_BASE_URL=
